<% layout('adminlayout') %>
<div class="row">
  <div class="medium-5 columns medium-offset-1" style="margin-top:2%">
    <% include ../partials/flash %>
  </div>
</div>
<div class="row">
  <div class="medium-12 columns">
    <div class="callout">
      <div><h3>Registered members</h3></div>
      <div class="callout secondary">
        <table style="width: 100%">
            <thead>
              <tr>
                <th>Count</th>
                <th>Firstname</th>
                <th>Lastname</th>
                <th>Username</th>
                <th>Email</th>
                <th>Shares</th>
                <th>Options</th>
              </tr>
            </thead>
            <tbody>
              <% members.forEach(function(member, index){ %>
              <tr>
                <th scope="row"><%= index + 1 %></th>
                <td><%= member.local.firstname %></td>
                <td><%= member.local.lastname %></td>
                <td><%= member.local.username %></td>
                <td><%= member.local.email %></td>
                <td><%= member.local.shares  %></td>
                <td>
                  <ul class="menu">
                    <li><a href="#" title="Delete"><i class="fa fa-trash " aria-hidden="true"></i> Remove</a></li>
                  </ul>
                </td>
              </tr>
              <%});%>
            </tbody>
          </table>
      </div>
    </div>
  </div>
</div>
